export const authenticated = document.documentElement.getAttribute('data-authenticated-account') === '';
export const debug = self.debug = true;
export const tau = Math.PI*2;

// Convenience for passing default arguments and the like.
export const __ = undefined;
